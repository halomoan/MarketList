<mvc:View
		controllerName="sap.ui.demo.masterdetail.controller.SplitApp"
		xmlns:mvc="sap.ui.core.mvc"
		displayBlock="true"
		busy="{appView>/busy}"
		busyIndicatorDelay="{appView>/delay}"
		xmlns="sap.m">
	<SplitApp id="idAppControl" mode="{device>/splitMode}"/>
</mvc:View>
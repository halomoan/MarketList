<mvc:View
		controllerName="sap.ui.demo.masterdetail.controller.Detail"
		xmlns="sap.m"
		xmlns:mvc="sap.ui.core.mvc"
		xmlns:semantic="sap.m.semantic">

	<semantic:DetailPage
			id="page"
			navButtonPress="onNavBack"
			showNavButton="{device>/system/phone}"
			title="{i18n>detailTitle}"
			busy="{detailView>/busy}"
			busyIndicatorDelay="{detailView>/delay}">

		<semantic:content>
			<ObjectHeader
					id="objectHeader"
					title="{MaterialText} + {Details/Detail1}"
					number="{
								path: 'Quantity',
								formatter: '.formatter.currencyValue'
					}"
					numberUnit="{UnitOfMeasure}">
			</ObjectHeader>
		
			<IconTabBar
					id="iconTabBar"
					class="sapUiResponsiveContentPadding">
				<items>
					<IconTabFilter
							id="iconTabBarFilter1"
							icon="sap-icon://hint"
							tooltip="{i18n>detailIconTabBarInfo}">
					</IconTabFilter>
					<IconTabFilter
							id="iconTabBarFilter2"
							icon="sap-icon://attachment"
							tooltip="{i18n>detailIconTabBarAttachments}">
					</IconTabFilter>
				</items>
			</IconTabBar>
			<Table
					id="lineItemsList"
					width="auto"
					updateFinished="onListUpdateFinished"
					noDataText="{i18n>detailLineItemTableNoDataText}"
					busyIndicatorDelay="{detailView>/lineItemTableDelay}"
					class="sapUiResponsiveMargin">
				<headerToolbar>
					<Toolbar id="lineItemsToolbar">
						<Title
								id="lineItemsHeader"
								text="{detailView>/lineItemListTitle}"/>
					</Toolbar>
				</headerToolbar>
				<columns>
					<Column>
						<Text text="{i18n>detailLineItemTableIDColumn}"/>
					</Column>
					<Column
							minScreenWidth="Tablet"
							demandPopin="true"
							hAlign="End">
						<Text text="{i18n>detailLineItemTableUnitNumberColumn}"/>
					</Column>
				</columns>
				<items>
					<ColumnListItem>
						<cells>
							<ObjectIdentifier
									title="{Details/Detail1}"
									text="{Details/Detail2}"/>
							<ObjectNumber
								number="{
									path: 'Details/Detail3',
									formatter: '.formatter.currencyValue'
								}"
								unit="{UnitOfMeasure}"/>
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</semantic:content>

		<semantic:positiveAction>
					<semantic:PositiveAction text="{i18n>addToList}" press="onSemanticButtonPress"/>
		</semantic:positiveAction>
		
		<semantic:negativeAction>
					<semantic:NegativeAction text="{i18n>close}" press="onSemanticButtonPress"/>
		</semantic:negativeAction>
				

	</semantic:DetailPage>

</mvc:View>